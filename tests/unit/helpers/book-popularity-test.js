
import { bookPopularity } from 'book-reviews/helpers/book-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | book popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bookPopularity([42]);
  assert.ok(result);
});

